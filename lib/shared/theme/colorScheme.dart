import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color lightBackground = Color(0xFFF5F7FA);
  static const Color lightCard = Color(0xFFEEF2FA);

  // Text
  static const Color lightText = Color(0xFF2D3142);
  static const Color lightSubText = Color(0xFF8A8D98);

  // Accent (Main Blue)
  static const Color accent = Color(0xFF4A80F0); // Main blue
  static const Color accentLight = Color(0xFF7AA5F7);
  static const Color accentDark = Color(0xFF3A6AE3);

  // Dark mode equivalents (optional for theme toggle)
  static const Color darkBackground = Color(0xFF1A1C2C);
  static const Color darkCard = Color(0xFF25273C);
  static const Color darkText = Color(0xFFE6E6EB);
  static const Color darkSubText = Color(0xFF9CA0AF);
}
